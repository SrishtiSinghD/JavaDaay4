package in.ac.sharda;

/*OnClickListener
    -onClick(Argument passed-clickEvent:-so its an interface as we don't what has happened)
    
OnChangeListener
    -onChange(Event Argument)*/

public interface IresultarrivedListener {

	public void resultArrived();

}
